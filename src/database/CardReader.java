package database;

public abstract class CardReader {
	
	/**
	 * reads the card and returns the account_id corresponding to that card
	 * @return
	 */
	static int readCard()
	{
		return 1;
	}
}
